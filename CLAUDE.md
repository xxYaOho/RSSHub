# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
pnpm dev                  # Dev server with hot reload on :1200
pnpm dev:cache            # Dev server with production caching enabled
pnpm build                # Full production build (routes + tsdown bundle)
pnpm start                # Run production build from dist/
pnpm test                 # Lint check + vitest with coverage
pnpm vitest:watch         # Vitest in watch mode, no lint
pnpm vitest:fullroutes    # Run all route integration tests
pnpm lint                 # oxlint type-aware check
pnpm format               # oxlint fix + oxfmt
```

## Architecture

RSSHub is an RSS feed aggregation server built with **Hono** (TypeScript). It scrapes web content and produces RSS/Atom/JSON feeds via a plugin-style route system.

### Route auto-discovery (`lib/registry.ts`)

Routes are NOT manually registered. They are auto-discovered from `lib/routes/<namespace>/`:

- **Dev** (`NODE_ENV=dev`): dynamic filesystem scan via `directoryImport()`
- **Production**: imports pre-built `assets/build/routes.js` (generated by `scripts/workflow/build-routes.ts`)

Each directory under `lib/routes/` becomes a namespace. Modules export one of three things:

- `route: Route` — a feed route (GET `/:namespace/:path`)
- `namespace: Namespace` — metadata for the namespace (name, url, lang)
- `apiRoute: APIRoute` — an API route (GET `/api/:namespace/:path`)

Literal path segments take priority over `:param` segments during sorting.

### Request middleware pipeline (`lib/app-bootstrap.tsx`)

Middleware runs in this exact order on every request:

1. `trimTrailingSlash` → `compress` → JSX renderer → logger → trace → honeybadger → sentry → `accessControl` → `debug` → **`template`** (converts `Data` to RSS/Atom/JSON output) → `header` → `antiHotlink` → `parameter` (query params: limit, format, filter etc.) → `cache`

Route handlers set `ctx.data` via `ctx.set('data', result)`. The `template` middleware reads `ctx.data` and renders the feed. If the handler returns a `Response` directly, the rest of the pipeline is skipped.

### Route file structure

Every route file exports a `route: Route` object and a `handler` function:

```ts
import type { Route } from '@/types';
import ofetch from '@/utils/ofetch';
import { parseDate } from '@/utils/parse-date';
import cache from '@/utils/cache';

export const route: Route = {
    path: '/topic/:id',
    categories: ['new-media'],
    example: '/mysite/topic/250',
    parameters: { id: '专题 id' },
    features: {
        requireConfig: false,
        requirePuppeteer: false,
        antiCrawler: false,
        supportBT: false,
        supportPodcast: false,
        supportScihub: false,
    },
    radar: [{ source: ['mysite.com/topic/:id'] }],
    name: '专题文章更新',
    maintainers: ['your-github-username'],
    handler,
};

async function handler(ctx) {
    const id = ctx.req.param('id');
    // fetch data, parse, build items...
    return { title: '...', link: '...', item: [...] };
}
```

The handler receives a Hono `Context` and returns a `Data` object (`{ title, link, description?, item: DataItem[] }`), or a `Response` for direct control.

### Namespace metadata

Each namespace directory needs a `namespace.ts`:

```ts
import type { Namespace } from '@/types';
export const namespace: Namespace = {
    name: '网站名',
    url: 'example.com', // no https:// prefix
    lang: 'zh-CN',
};
```

### Key utilities (all under `lib/utils/`)

| Import                                | Use                                          |
| ------------------------------------- | -------------------------------------------- |
| `ofetch` from `@/utils/ofetch`        | HTTP requests (preferred over `got`)         |
| `got` from `@/utils/got`              | HTTP requests (legacy, still widely used)    |
| `cache` from `@/utils/cache`          | `cache.tryGet(key, fn)` for per-item caching |
| `parseDate` from `@/utils/parse-date` | Parse date strings to timestamps             |
| `config` from `@/config`              | Runtime config (env vars, defaults)          |

### Config system (`lib/config.ts`)

All config via environment variables. `config` is a frozen object computed at import time. Key env vars:

- `PORT` (default 1200), `CACHE_TYPE` (memory/redis), `REDIS_URL`
- `ACCESS_KEY` — if set, required as `?key=` or `Authorization: Bearer` header
- Route-specific credentials: `BILIBILI_COOKIE_*`, `GITHUB_ACCESS_TOKEN`, `TWITTER_*`, etc.
- `DEBUG_INFO` (default `'true'`) — enables debug endpoints
- **Translation**:
    - `TRANSLATE_GEMMA_ENDPOINT` / `TRANSLATE_GEMMA_API_KEY` / `TRANSLATE_GEMMA_MODEL` — TranslateGemma (LM Studio). **Endpoint must include `/v1` suffix** (e.g. `http://host:1234/v1`), code appends `/chat/completions`.
    - `TRANSLATE_GEMMA_MAX_INPUT_TOKENS` (default 1200) — chunk size limit
    - `TRANSLATE_GEMMA_PROMPT` — e.g. "Translate from English to Simplified Chinese."
    - `OPENAI_API_ENDPOINT` / `OPENAI_API_KEY` / `OPENAI_MODEL` — chatgpt translation (DeepSeek, OpenAI, etc.)
    - `OPENAI_FALLBACK_API_ENDPOINT` / `OPENAI_FALLBACK_API_KEY` / `OPENAI_FALLBACK_MODEL` — fallback LLM

## Critical conventions

- **Never add routes to `lib/router.js`** — it's deprecated. All routes go in `lib/routes/<namespace>/`.
- **Use `import type`** for type-only imports. Sort imports (oxlint enforces this).
- **Always wrap per-item detail fetching in `cache.tryGet()`**.
- **`example` must start with `/`** — it's a route path, not a full URL.
- **`radar[].source` has no `https://` prefix** — use `'www.example.com/path'`.
- **Single category** in `categories` array (pick one from the `Category` union in `lib/types.ts`).
- **No custom pagination or filtering** — RSSHub has built-in `?limit=`, `?filter=`, etc.
- **Prefer API endpoints over HTML scraping** when the target site has one.
- **`description` is content only** — title, author, pubDate go in their own fields.
- **Don't fake dates** — leave `pubDate` undefined if the source has no date.
- **Writing tests:** Use `vitest` with `msw` for HTTP mocking. Route integration tests live in `lib/routes.test.ts`.
- **No `README.md` or separate `radar.ts`** in route directories — use `Route.description` and `Route.radar`.

## TypeScript

- Path alias `@/` maps to `lib/`
- `Data`, `DataItem`, `Route`, `Namespace` types are in `lib/types.ts`
- `ctx` parameter in handlers is Hono's `Context` type (import from `hono` if needed)

## Developing a new route — practical workflow

### 1. Research the target site

按优先级依次尝试:

1. **WordPress REST API**: `/wp-json/wp/v2/posts` — 如果可用,直接调 API
2. **内嵌 JSON**: 查看 HTML 中 `<script>` 标签是否有 `var xxx = "[{...}]"` 格式的数据,用 `response.match(/var foo = "(.+?)";/s)` 提取,然后 `JSON.parse(JSON.parse(`"${raw}"`))` 解码
3. **静态 HTML**: 用 `ofetch` + `cheerio` 抓取
4. **JS 动态渲染**: 如果静态 HTML 缺内容,用 Playwright 浏览器打开确认 DOM 结构,再决定是否需要 `requirePuppeteer`

用 Playwright (`browser_navigate` + `browser_evaluate`) 看真实 DOM,不要仅依赖 WebFetch (它只返回渲染文本,不反映 JS 动态内容)。

### 2. Choose the scraping strategy

- **列表有日期/作者** → 直接从列表提取,不进详情页
- **列表只有标题+链接** → 两阶段: 列表提取 + `cache.tryGet()` 进入详情页补全 date/description
- **每篇文章内容都不同** → 必须进详情页取 description

### 3. Test as you go

```bash
# Direct Hono app test (fast, no network deps)
echo "import app from './app.js'; const r = await app.request('http://localhost/uisdc/news'); console.log(r.status)" | pnpm tsx -

# Curl test against running server
curl -s --noproxy '*' http://localhost:1200/uisdc/news | head -20
```

**注意**: macOS 有代理环境变量 (`http_proxy`) 可能干扰 curl。加 `--noproxy '*'` 避免。

生产 (1200) 和开发 (1300) 端口分离，测试新路由指向 1300 端口。用 `lsof -i :1300` 确认 dev server 在监听。

### 4. 部署模式

RSSHub 区分**开发**和**生产**两种运行模式。生产和开发共享同一个 Redis（Docker 容器，端口 6379，不经过 Docker 网络，宿主机直接 `localhost:6379` 访问）。

**启动 Redis**（两个环境都需要）：

```bash
docker compose up -d redis
```

#### 生产环境 — master 分支，端口 1200，pm2 守护

Master 分支存放稳定代码，由 pm2 管理进程（掉线自动重启、开机自启），通过 `tsx` 直接运行 TypeScript 源码，无需 build。

```bash
# 启动（需要先 source ~/.zshrc 加载 DEEPSEEK 环境变量）
source ~/.zshrc && pm2 start "env \
  PORT=1200 \
  CACHE_TYPE=redis \
  REDIS_URL=redis://localhost:6379/ \
  CACHE_EXPIRE=2100 \
  OPENAI_API_ENDPOINT=\${DEEPSEEK_BASE_URL}/v1 \
  OPENAI_API_KEY=\${DEEPSEEK_API_KEY} \
  OPENAI_MODEL=deepseek-v4-flash \
  OPENAI_INPUT_OPTION=bilingual \
  OPENAI_MAX_TOKENS=16384 \
  'OPENAI_PROMPT_TITLE=Translate the following title into Simplified Chinese. Reply with ONLY the translation, nothing else.' \
  'OPENAI_PROMPT=Translate the following content into Simplified Chinese. Reply with ONLY the translation, nothing else.' \
  OPENAI_FALLBACK_API_ENDPOINT=http://100.106.114.92:1234/v1 \
  OPENAI_FALLBACK_API_KEY=lmstudio \
  OPENAI_FALLBACK_MODEL=qwen3.6-35b-a3b \
  TRANSLATE_GEMMA_ENDPOINT=http://100.106.114.92:1234/v1 \
  TRANSLATE_GEMMA_API_KEY=lmstudio \
  TRANSLATE_GEMMA_MODEL=translategemma-12b-it \
  TRANSLATE_GEMMA_MAX_INPUT_TOKENS=1200 \
  'TRANSLATE_GEMMA_PROMPT=Translate from English to Simplified Chinese.' \
  TRANSLATE_HYMT_ENDPOINT=http://localhost:1234/v1 \
  TRANSLATE_HYMT_API_KEY=lmstudio \
  TRANSLATE_HYMT_MODEL=hy-mt2-7b \
  'LMSTUDIO_AUTO_UNLOAD=false' \
  npx tsx lib/index.ts" --name rsshub

# 管理
pm2 restart rsshub   # 重启（注意：restart 不更新环境变量，需改用 delete + start）
pm2 stop rsshub      # 停止
pm2 logs rsshub      # 查看日志
pm2 status           # 查看状态
```

部署流程：`worktree.dev 开发 → merge 到 master → pm2 restart rsshub`

**注意**: `pm2 restart --update-env` **不会**重新读取 `.env` 文件（只更新当前 shell 环境变量）。修改 `.env` 后需 `pm2 delete rsshub && pm2 start ...` 重建进程，或直接在 `pm2 start` 命令中通过 `--env` 传入变更的变量。

#### 开发环境 — worktree.dev 分支，端口 1300，热重载

通过 git worktree 创建独立的 dev 分支目录，使用 `pnpm dev`（tsx watch）热重载。

```bash
# 1. 创建 worktree（首次）
git worktree add -b dev /Users/teatin/.worktree/RSSHub/dev master

# 2. 安装依赖（首次）
cd /Users/teatin/.worktree/RSSHub/dev && pnpm install

# 3. 启动开发服务器
cd /Users/teatin/.worktree/RSSHub/dev
PORT=1300 CACHE_TYPE=redis REDIS_URL=redis://localhost:6379/ pnpm dev

# 访问：http://localhost:1300
```

如需翻译功能，追加翻译相关环境变量。

**双语翻译开发配置**（DeepSeek API 为主，LM Studio fallback）：

```bash
source ~/.zshrc
PORT=1300 screen -dmS rsshub-dev env \
  OPENAI_API_ENDPOINT="${DEEPSEEK_BASE_URL}/v1" \
  OPENAI_API_KEY="${DEEPSEEK_API_KEY}" \
  OPENAI_MODEL="deepseek-v4-flash" \
  OPENAI_INPUT_OPTION="bilingual" \
  OPENAI_MAX_TOKENS="16384" \
  OPENAI_PROMPT_TITLE="Translate the following title into Simplified Chinese. Reply with ONLY the translation, nothing else." \
  OPENAI_PROMPT="Translate the following content into Simplified Chinese. Reply with ONLY the translation, nothing else." \
  OPENAI_FALLBACK_API_ENDPOINT="http://100.106.114.92:1234/v1" \
  OPENAI_FALLBACK_API_KEY="lmstudio" \
  OPENAI_FALLBACK_MODEL="qwen3.6-35b-a3b" \
  TRANSLATE_GEMMA_ENDPOINT="http://100.106.114.92:1234/v1" \
  TRANSLATE_GEMMA_API_KEY="lmstudio" \
  TRANSLATE_GEMMA_MODEL="translategemma-12b-it" \
  TRANSLATE_GEMMA_MAX_INPUT_TOKENS="1200" \
  TRANSLATE_GEMMA_PROMPT="Translate from English to Simplified Chinese." \
  CACHE_TYPE=redis REDIS_URL=redis://localhost:6379/ \
  pnpm dev
```

**环境对比**：

| 环境 | 端口 | 分支           | 启动方式  | 特点               |
| ---- | ---- | -------------- | --------- | ------------------ |
| 生产 | 1200 | master         | pm2 + tsx | 进程守护，开机自启 |
| 开发 | 1300 | dev (worktree) | pnpm dev  | 热重载，独立目录   |

#### 翻译路由参数

| 参数              | 说明                                                        | 示例                                        |
| ----------------- | ----------------------------------------------------------- | ------------------------------------------- |
| `?chatgpt`        | DeepSeek 整篇翻译                                           | `/proxy/rss?url=...&chatgpt&limit=5`        |
| `?translatehymt`  | Hy-MT2 专用翻译（固定中文，本机 LM Studio）                 | `/proxy/rss?url=...&translatehymt&limit=1`  |
| `?translategemma` | TranslateGemma 分段翻译（默认英译中，`=jp` 等指定目标语言） | `/proxy/rss?url=...&translategemma&limit=1` |
| `?llmgemma`       | 通用 LLM 整篇翻译（`=jp` 等指定目标语言）                   | `/proxy/rss?url=...&llmgemma&limit=1`       |
| `?autots`         | 智能翻译（translatehymt 优先，chatgpt 回退）                | `/proxy/rss?url=...&autots=jp&limit=1`      |

`?autots` 支持语言代码：`cn/zh` (中文), `jp/ja` (日文), `en` (英文), `ko` (韩文), `fr` (法文), `de` (德文)。不传值默认为 `cn`。

`?autots=cn` 走 Hy-MT2（本机，固定中文），其余语言跳过 Hy-MT2 直接走 DeepSeek。

**LM Studio 模型生命周期**：

- 翻译前自动预热：模型未加载时先发单请求排队加载（LM Studio 未加载时并发请求会立即 500），加载完成后才并发翻译
- 翻译后自动卸载：`LMSTUDIO_AUTO_UNLOAD`（默认 `false` = 模型常驻）。设为 `true` 时，翻译完成 30 秒后自动卸载模型释放显存。生产环境 Hy-MT2 常驻，无需设置

**RSS 代理路由**: `/proxy/rss?url=<外部RSS地址>`

- 抓取任意外部 RSS/Atom feed，透传 RSSHub 中间件
- 配合翻译参数实现多语言 RSS

**Benchmark** (2 篇文章, clean cache):

| 模型               | 耗时/篇 | 年费   | 质量 |
| ------------------ | ------- | ------ | ---- |
| DeepSeek V4 Flash  | ~26s    | <$0.10 | 最佳 |
| qwen2.5-14b (本地) | ~48s    | 免费   | 良好 |
| qwen3.6-35b (本地) | ~125s   | 免费   | 较好 |

### 5. Clean up after development

```bash
# 删除临时测试脚本
rm -f lib/test-*.ts

# 确保没有残留旧进程占用端口
lsof -i :1300 2>/dev/null | grep LISTEN
lsof -i :1200 2>/dev/null | grep LISTEN

# 重新干净启动开发服务器
PORT=1300 CACHE_TYPE=redis REDIS_URL=redis://localhost:6379/ pnpm dev
```

**常见残留进程来源:**

- `tsx watch` 的子进程不会随终端关闭自动退出，多次 `pnpm dev &` 会积累
- `npx tsx lib/index.ts` 前台/后台测试后，子进程（实际 node 进程）可能残留，需用 `ps aux | grep tsx` 排查并 `kill`
- pm2 占用 1200 端口（正常的生产进程，不是残留）

规律: 如果 curl 返回 `503` + "Welcome to RSSHub!" HTML，说明端口上的进程不是当前 dev server，用 `lsof -i :1300` 排查。

## Known Issues

### 缓存过期时间与 RSS 阅读器刷新间隔不匹配导致超时

**场景**: Reeder 等 RSS 阅读器设置 30 分钟自动刷新，但 RSSHub 默认缓存仅 5 分钟 (`CACHE_EXPIRE=300`)。每次阅读器请求时缓存都已过期，触发冷缓存抓取。在家庭网络环境下（如 Mac Mini + Tailscale），冷抓取偶发超时。

**修复**: 将 `CACHE_EXPIRE` 调到比阅读器刷新间隔稍长（如 35 分钟 / 2100 秒），确保每次请求命中缓存。已纳入生产 pm2 启动命令。

### ~~Cache key 未包含翻译参数~~（已修复，commit 532dd9886）

`lib/middleware/cache.ts` 的缓存 key 已包含 `chatgpt`/`autots`/`translategemma`/`translatehymt`/`llmgemma` 及语言代码（如 `:autots=jp`、`:translategemma=jp`），带翻译与不带翻译的请求使用独立的 `controlKey`，不再互相阻塞或污染缓存。

注意：`translategemma` 语言代码纳入 controlKey 后，旧缓存 key 的哈希变化，升级后首次请求会重新生成，属正常现象。
